<?php
/**
 * Admin helper functions for bbPress Report Abuse
 *
 * Drop this in the plugin's inc/ directory and the main plugin file will use WP Settings API hooks (already added).
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

/* This file intentionally left minimal — settings are registered in the main plugin file.
   Add any additional admin helpers here, e.g., email notification UI, report archive, etc.
*/
